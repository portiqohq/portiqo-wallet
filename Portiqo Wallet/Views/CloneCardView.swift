import SwiftUI

struct CloneCardView: View {
    var body: some View {
        VStack {
            Text("This is where you would clone a Keycard")
            Text("But there's nothing here yet :(")
        }
    }
}

#Preview {
    CloneCardView()
}
